<ul class="taglist">
{% for tag in collections | getKeys | filterTagList | alphabetically %}
    {% set tagUrl %}/tags/{{tag | slugify}}/{% endset %}
    <li><a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a></li>
{% endfor %}
</ul>
